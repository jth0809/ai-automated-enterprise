# Claude Routing Rules

To save tokens, do NOT load global context unless requested.
1. When modifying code, ONLY read the `AGENTS.md` file located in your current working directory (e.g., `apps/frontend/AGENTS.md`).
2. Do NOT read `ARCHITECTURE_PLAN.md` unless making repository-wide architectural changes.
