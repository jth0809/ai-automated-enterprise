apiVersion: v1
kind: Namespace
metadata:
  name: redis
  labels:
    # Enroll Redis in Istio Ambient Mesh: cache traffic from the backend
    # is wrapped in L4 mTLS by ztunnel (HBONE, port 15008 — already
    # whitelisted in the backend CiliumNetworkPolicy).
    istio.io/dataplane-mode: ambient
